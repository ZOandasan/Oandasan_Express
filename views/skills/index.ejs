<%- include('../partials/header') %>
<div>
    <ul>
      <% skills.forEach(function(s) { %>
        <li>
          <a href="/skills/<%= s.id %>"><%= s.skill %></a>
            - 
          <%= s.classType === 'CIT'  ? 'CIT' : 'DMD' %>
        </li>
      <% }); %>
    </ul>
</div>
<%- include('../partials/footer') %>